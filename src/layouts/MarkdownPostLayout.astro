---
import Layout from "./Layout.astro";
import Panel from "../components/Panel.astro";
const { frontmatter } = Astro.props;
---
<Layout title={frontmatter.title}>
    <Panel tone="dark">
    <h1 class="text-center text-bone">{frontmatter.title}</h1>
    <p class="text-sm font-mono uppercase tracking-widest text-amber text-center mb-6">{new Date(frontmatter.pubDate).toLocaleDateString()}</p>

    <slot />

    <!--<div class="flex-1 border-b border-gray-400 py-2"></div>-->

    <!--<div class="tags">-->
    <!--    {frontmatter.tags.map((tag: string) => (-->
    <!--            <p class="tag"><a href={`/tags/${tag}`}>{tag}</a></p>-->
    <!--    ))}-->
    <!--</div>-->
    </Panel>
</Layout>