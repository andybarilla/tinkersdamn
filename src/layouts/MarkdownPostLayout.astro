---
import Layout from "./Layout.astro";
const { frontmatter } = Astro.props;
---
<Layout>
    <p class="text-2xl font-bold text-atlantis-400 text-center">{frontmatter.title}</p>
    <p class="text-sm text-gray-400 text-center">{new Date(frontmatter.pubDate).toLocaleDateString()}</p>

    <slot />

    <!--<div class="flex-1 border-b border-gray-400 py-2"></div>-->

    <!--<div class="tags">-->
    <!--    {frontmatter.tags.map((tag: string) => (-->
    <!--            <p class="tag"><a href={`/tags/${tag}`}>{tag}</a></p>-->
    <!--    ))}-->
    <!--</div>-->

</Layout>